info:
  name: signin
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:3000/dropbox/signin
  headers:
    - name: operational-setup-key
      value: "{{operational-setup-key}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

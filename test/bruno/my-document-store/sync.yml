info:
  name: sync
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/admin/sync
  headers:
    - name: api_key
      value: "{{api_key}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
